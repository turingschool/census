<table class="table table-hover" data-toggle="table">
  <thead>
    <tr>
      <th data-sortable="true">Name</th>
      <th data-sortable="true"># of Students</th>
      <th data-sortable="true">Status</th>
    </tr>
  </thead>

  <% cohorts.each do |cohort| %>
    <tr class="cohort">
      <td><%= cohort.name %></td>
      <td><%= cohort.student_count %></td>
      <td><%= cohort.status %></td>
    </tr>
  <% end %>
</table>
